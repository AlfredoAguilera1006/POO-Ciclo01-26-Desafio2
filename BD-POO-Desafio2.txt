-- Crear la base de datos
CREATE DATABASE IF NOT EXISTS biblioteca_db;
USE biblioteca_db;

-- Crear la tabla autor
CREATE TABLE autor (
    id_autor INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
    nacionalidad VARCHAR(50) NOT NULL
);

-- Crear la tabla categoria
CREATE TABLE categoria (
    id_categoria INT AUTO_INCREMENT PRIMARY KEY,
    nombre_categoria VARCHAR(50) NOT NULL
);

-- Crear la tabla libro con llaves foráneas apuntando a autor y categoria
CREATE TABLE libro (
    id_libro INT AUTO_INCREMENT PRIMARY KEY,
    titulo VARCHAR(150) NOT NULL,
    año_publicacion INT NOT NULL,
    id_autor INT,
    id_categoria INT,
    FOREIGN KEY (id_autor) REFERENCES autor(id_autor),
    FOREIGN KEY (id_categoria) REFERENCES categoria(id_categoria)
);

-- Insertar datos de prueba para los Autores
INSERT INTO autor (nombre, nacionalidad) VALUES 
('Gabriel García Márquez', 'Colombiana'),
('J.R.R. Tolkien', 'Británica'),
('Isabel Allende', 'Chilena');

-- Insertar datos de prueba para las Categorías
INSERT INTO categoria (nombre_categoria) VALUES 
('Realismo Mágico'),
('Fantasía'),
('Novela Histórica');

-- Insertar datos de prueba para los Libros
-- Nota: Los id_autor e id_categoria corresponden a los insertados arriba
INSERT INTO libro (titulo, año_publicacion, id_autor, id_categoria) VALUES 
('Cien años de soledad', 1967, 1, 1),
('El Señor de los Anillos: La Comunidad del Anillo', 1954, 2, 2),
('La casa de los espíritus', 1982, 3, 1),
('El amor en los tiempos del cólera', 1985, 1, 1),
('Inés del alma mía', 2006, 3, 3),
('El hobbit', 1937, 2, 2);